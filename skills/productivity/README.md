# Productivity

Skills about using oh-my-pi's own tooling well, not tied to any specific engineering task.

- **[using-ohmypi](./using-ohmypi/SKILL.md)** — How to use the `ohmypi` CLI and `jobs.json`: when to add a job, how `mode`/`extensions`/`skills`/`theme`/`contextFile` fields work, and how `run`/`toggle`/`list`/`context` differ.
